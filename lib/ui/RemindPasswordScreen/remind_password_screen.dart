import 'package:flutter/material.dart';

class RemindPasswordScreen extends StatelessWidget {
  const RemindPasswordScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Remind Password Screen");
  }
}
